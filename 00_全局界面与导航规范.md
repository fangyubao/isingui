# OptiQubo 全局界面与导航规范

## 一级菜单

所有页面左侧一级菜单固定为：

```text
首页
QUBO 求解
经典求解
结果分析
设置
```

底部固定保留“帮助”。当前页面使用蓝色高亮；菜单顺序、宽度、图标尺寸和文字间距不得因页面变化而改变。

## 设置二级菜单

“标定”和“设备与系统”不再作为一级菜单。进入“设置”后显示：

```text
设置
├── 标定
└── 设备与系统
```

## 顶部与底部

顶部固定包含唯一 Logo、产品名称、页面标题、主题切换、设置快捷按钮和窗口控制；不设置搜索框。底部状态栏统一显示版本、系统状态、USB 与设备摘要。

## Sigma 统一规则

Sigma 是一维向量：

\[
\boldsymbol{\sigma}=[\sigma_0,\sigma_1,\ldots,\sigma_{N-1}],\qquad \sigma_i\in\{-1,+1\}
\]

所有结果分析视图统一使用索引 + `+1 / -1` 卡片；经典结果若保存的是二进制向量，则先转换：

\[
\sigma_i=2x_i-1
\]

## 结果分析约束

不显示“是否达到全局最优”“是否达到最优解”等判定。对比模式仅在 QUBO 文件或任务 ID、矩阵维度、变量数和矩阵哈希一致时启用能量差与 Gap。
