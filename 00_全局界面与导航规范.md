# OptiQubo 全局界面与导航规范

## 1. 文档范围

本规范适用于以下已确定页面：

1. 首页
2. QUBO 求解
3. 经典求解
4. 设置
   - 标定
   - 设备与系统

“结果分析”页面已单独整理，不在本文件组内。

---

## 2. 全局一级菜单

所有页面的左侧一级菜单必须保持完全一致：

```text
首页
QUBO 求解
经典求解
结果分析
设置
```

左侧栏底部固定保留：

```text
帮助
```

### 2.1 菜单规则

- 当前页面使用蓝色高亮；
- 左侧栏宽度固定；
- 图标、文字字号和间距固定；
- 不因页面内容不同而增删菜单项；
- “标定”和“设备与系统”不再作为一级菜单；
- 顶部齿轮图标可作为设置快捷入口，但不能替代左侧“设置”。

---

## 3. 设置二级菜单

进入“设置”后，在全局侧边栏右侧增加设置二级栏：

```text
设置
├── 标定
└── 设备与系统
```

### 3.1 设置页规则

- 全局一级菜单中的“设置”高亮；
- 当前二级菜单高亮；
- 标定与设备控制页面共享同一设置容器；
- 切换二级菜单时顶部栏和全局侧边栏不变化。

---

## 4. 顶部栏

顶部栏固定包含：

- 左上角唯一产品 Logo；
- 产品名称 `OptiQubo`；
- 当前页面标题；
- 明亮 / 暗黑主题切换；
- 设置快捷按钮；
- 最小化；
- 最大化 / 还原；
- 关闭。

不设置顶部搜索框。

产品名称文字的视觉权重低于 Logo，不重复放置第二个 Logo。

---

## 5. 底部状态栏

底部状态栏在所有页面保持一致，可显示：

```text
OptiQubo v1.0.0
系统状态
USB 连接状态
DMD 状态
SLM 状态
PD 状态
当前时间
```

页面运行任务时，可临时显示：

```text
当前任务
运行进度
当前最优能量
```

---

## 6. 统一视觉规范

- 宽屏桌面应用布局；
- 浅色背景与白色圆角 Card；
- 蓝色为主强调色；
- 绿色表示正常、已连接、已完成；
- 红色表示停止、异常或危险操作；
- 紫色用于保存、结果和对比类操作；
- 输入框、按钮、图表标题和 Card 标题尺寸统一；
- 页面窗口尺寸统一；
- 左侧栏、顶部栏和底部栏位置固定；
- 支持明亮 / 暗黑主题；
- 不为填充空间增加无实际作用的控件。

---

## 7. 术语统一

统一使用：

```text
QUBO 求解
经典求解
结果分析
设置
标定
设备与系统
Sigma 向量
全局最优能量
已遍历状态数
总状态数
```

不使用：

```text
模拟求解
暴力求解
暴力穷举
经典暴力
求解算法
```

Sigma 始终表示一维向量，不以矩阵形式描述。
